# Required. Get a key at https://typesafe.ai. Used server-side only; it never reaches the browser.
TYPESAFE_API_KEY=

# Optional. A GitHub token raises the API rate limit from 60 to 5,000 requests an hour.
# A token with no scopes is enough: Mergeworthy only reads public repositories.
GITHUB_TOKEN=
